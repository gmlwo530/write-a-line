import 'package:flutter/material.dart';

class More extends StatelessWidget {
  More();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('More'),
        ),
        body: new Center(
          child: Text("CHOI HEE JAE")
        )
    );
  }
}
